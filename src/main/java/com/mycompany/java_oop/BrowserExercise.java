package com.mycompany.java_oop;

/**
 * Your OOP exercise — not kindergarten :-)
 *
 * Don't just define the four concepts. Use them.
 *
 * Create an abstract/interface-based system for browsers:
 *
 *   Browser
 *    ├── Chrome
 *    └── Firefox
 *
 * Browser should have: start()
 * Chrome prints:  Chrome started
 * Firefox prints: Firefox started
 *
 * In main() demonstrate polymorphism, something like:
 *
 *   Browser browser = new Chrome();
 *   browser.start();
 *
 * Then switch it to Firefox and call start() again.
 *
 * Write the whole thing yourself. This hits interfaces,
 * implementation, objects, methods, and polymorphism in one shot.
 *
 * Stuck? Re-read LEARN_OOP.md section 5 (payments) and section 7.
 * Payments and browsers are the same shape with different names.
 * Feeling brave? Make Browser an abstract class (like Animal) instead.
 */
public class BrowserExercise {

}
